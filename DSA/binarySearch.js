const arr = [2, 5, 8, 12, 16, 23, 38];
let target = 16;
let left = 0;
let mid = math.floor((left + right) / 2);
let right = arr.length - 1;
while (left <= right) {
    if (arr[left] > target) {
        
        
    }
}
